<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:paddingBottom="@dimen/padding_vertical_margin"
    android:paddingLeft="@dimen/padding_horizontal_margin"
    android:paddingRight="@dimen/padding_horizontal_margin"
    android:paddingTop="@dimen/padding_vertical_margin"
    tools:context=".Splash"
    android:background="@drawable/splash_blackground" >

    <ImageView
        android:id="@+id/activity_splash_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/activity_splash_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/activity_splash_logo"
        android:layout_alignBottom="@id/activity_splash_logo"
        android:text="@string/app_name"
        android:textSize="@dimen/font_very_large" />

</RelativeLayout>